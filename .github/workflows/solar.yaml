name: Solar System
on: push
jobs:
  unit-testing:
    runs-on: ubuntu-latest
    steps:
      - name: Clone The Repo
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18 

      - name: Install dependencies
        run: npm install
    
      - name: Install npm Test
        run: | 
            echo "DB_USERNAME=${{ secrets.MONGO_USERNAME }}" >> $GITHUB_ENV
            echo "DB_PASSWORD=${{ secrets.MONGO_PASSWORD }}" >> $GITHUB_ENV
            echo "DB_URI=${{ secrets.MONGO_URI }}" >> $GITHUB_ENV

      - name: Run tests
        run: npm test


